#// FIXME usually in test application-test.yaml is used for test profile
#// FIXME try to study - how application.yaml from main and test acts in thta case? which has more priority?
spring:

  liquibase:
    enabled: false

  datasource:
    url: jdbc:h2:file:./testDB;AUTO_SERVER=TRUE
    username: admin
    driverClassName: org.h2.Driver

  jpa:
    database-platform:
      org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        ddl-auto: none
    open-in-view: false
    show-sql: false

  sql:
    init:
      mode: never